{
  "name": "ereader-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ereader-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute ereader-db --file=./schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.6.9",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.6.3",
    "@cloudflare/workers-types": "^4.20241022.0"
  }
}
